# This file contains metadata for your plugin.

[general]
name=QGIS RORB
qgisMinimumVersion=3.22
qgisMaximumVersion=4.99
description=Prepare GIS layers and build RORB/URBS control vectors from a catchment.
version=1.8
author=Tom Norman
email=tom@ncos.com.au

about=RORB Catchment Tools: A combined plugin for preparing and building RORB and URBS
    runoff model control vectors from catchment GIS layers.

    Preparation tools:
    - Auto-name subcatchment polygons south to north (numeric IDs)
    - Auto-name centroid points south to north (letter IDs)
    - Auto-name confluence points south to north (lowercase letter IDs)
    - Auto-name reaches from connected node IDs
    - Check RORB layer link consistency
    - Clean up negative slope values in reach layers

    Build tools:
    - Build RORB control vector (.cat file)
    - Build URBS control vector (.vec + .cat files)

    Bundles the pyromb (>=0.3) library for the build tools — no separate
    install needed.

tracker=https://github.com/norman-tom/pyromb/issues
repository=https://github.com/norman-tom/runoff-model

hasProcessingProvider=yes

changelog=
v1.7: Run RORB: fix ISA group parsing — Parameters table now shows the correct
    7.2 print nodes and outlet from the .catg (was showing only "outlet").
    Add Refresh button in Parameter configuration to re-read ISA groups from
    the current .catg without re-browsing.
v1.6: Run RORB: fix Parameters table in non-lumped (vary) mode — rows now show
    ISA groups (matching RORBwin's Parameter Specification dialog) instead of
    individual sub-areas.
v1.5: Run RORB: add "Suggest Kc" helper dialog — choose a regional equation
    (ARR Book 7 area-based or Dav-based Pearse et al.) or the RORB Manual
    default (2.2 × √A) to compute a starting kc for the selected ISA group,
    then adopt it into the Parameters table with one click.
v1.4: Build .catg: fix sub-catchment areas being wrong (stuck at basin[0]
    default) for some polygons — replace the id-field polygon lookup with a
    direct centroid→polygon spatial join that matches how name_centroids()
    works; also fix total-area double-counting when a polygon contains two
    centroid points.
v1.3: (internal release — skipped to align with suite v2.9)
v1.2: Fix print-node location names being dropped (and stray "NULL" text)
    when building .catg files; shorten the Build .catg pipeline dialog and
    simplify its stats sidebar.
v1.1: Add "Run RORB" dialog — runs RORB_CMD.exe against a .catg/.stm pair
    with per-interstation-area kc/m/IL/CL, and hands the .out file off to
    the Results Viewer. Bundle pyromb so users no longer need to pip
    install it manually.
v1.0: Initial release - combined runoff_model and rorb_specific tools.

tags=runoff,hydrology,stormwater,rorb,urbs,wbnm,catchment

homepage=https://github.com/norman-tom/runoff-model
category=Vector
icon=icon.png
experimental=False
deprecated=False
server=False
